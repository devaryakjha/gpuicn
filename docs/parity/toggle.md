# Toggle parity

- Upstream: shadcn/ui `4.19.0` Nova Toggle.
- gpuicn: `registry/toggle/toggle.rs`.
- Base GPUI supplies pointer, keyboard, pressed state, and disabled guards. The port uses the Nova default and outline visual variants.
