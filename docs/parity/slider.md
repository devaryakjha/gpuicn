# Slider parity

- Upstream: shadcn/ui `4.19.0` Nova Slider.
- gpuicn: `registry/slider/slider.rs`.
- Base GPUI supplies pointer dragging, keyboard stepping, value clamping, focus, and accessibility. This wrapper presents one thumb; use Base GPUI directly for a range slider until a range wrapper is added.
